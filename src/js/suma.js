export function suma(a, b) {
  return a + b;
}

/* En el Json type: "module"  es para que agarre el export y el import ademas de correrlo con el comando node --experimental-vm-modules node_modules/jest/bin/jest.js*/